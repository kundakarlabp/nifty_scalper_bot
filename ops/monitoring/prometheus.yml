global:
  scrape_interval: 15s
  evaluation_interval: 30s

rule_files:
  - /etc/prometheus/alert_rules.yml

scrape_configs:
  - job_name: "nifty_scalper_bot"
    metrics_path: /metrics
    static_configs:
      - targets:
          - "bot:8000"
