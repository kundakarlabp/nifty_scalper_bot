name: Validate Market-Aware Optimisations

on:
  pull_request:

permissions:
  contents: write

jobs:
  targeted-entry-path-tdd:
    if: github.head_ref == 'fix/entry-policy-critical-feed-20260804'
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/entry-policy-critical-feed-20260804
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install project
        run: pip install -e '.[dev]'
      - name: Add focused regressions first
        run: |
          cat >> tests/strategies/test_runner_symbol_role_gate.py <<'PY'


def test_expiry_entry_policy_blocks_before_live_readiness_and_preparation(monkeypatch):
    runner_obj, strategy_manager, risk_manager, order_manager, _selected_ce = (
        _build_phase9_runner(monkeypatch)
    )
    runner_obj._trigger_candidate_symbols = {"NSE:NIFTY"}
    runner_obj._emit_no_trade_decision = Mock()
    monkeypatch.setattr(
        "nifty_scalper_bot.strategies.runner.expiry_theta_block",
        lambda: (True, "expiry_day_after_13:30_ist"),
    )

    runner_obj._on_tick(
        "NSE:NIFTY",
        {
            "symbol": "NSE:NIFTY",
            "last_price": 24000.0,
            "timestamp": time.time(),
            "trace_id": "expiry-entry-pregate",
            "source": "ws",
        },
    )

    strategy_manager.generate_signal.assert_called_once()
    risk_manager.validate.assert_not_called()
    order_manager.submit.assert_not_called()
    assert any(
        call.kwargs.get("stage") == "phase10_entry_policy"
        and call.kwargs.get("reason") == "expiry_day_after_13:30_ist"
        and call.kwargs.get("allowed") is False
        for call in runner_obj._emit_runner_eval_decision.call_args_list
    )
    runner_obj._emit_no_trade_decision.assert_called_once_with(
        symbol="NFO:NIFTY26JUN24000CE",
        trace_id="expiry-entry-pregate",
        category="entry_session_policy",
        reason="expiry_day_after_13:30_ist",
    )
PY
          cat >> tests/data/test_mdm_tick_coalescing.py <<'PY'


def test_optional_context_option_is_not_recovery_critical():
    mdm = MarketDataManager(kite=None)
    mapping = _wire_symbols(mdm)
    selected_ce = "NFO:NIFTY26JUN24000CE"
    selected_pe = "NFO:NIFTY26JUN24000PE"
    optional_ce = "NFO:NIFTY26JUN25000CE"
    mdm.set_active_contract_basket(
        {
            "all_tokens": list(mapping),
            "token_by_symbol": {symbol: token for token, symbol in mapping.items()},
            "spot_symbol": "NSE:NIFTY",
            "futures_symbol": "NFO:NIFTY26JUNFUT",
            "selected_ce": selected_ce,
            "selected_pe": selected_pe,
            "option_symbols": [selected_ce, selected_pe, optional_ce],
        }
    )

    required = mdm._required_live_symbols()

    assert "NFO:NIFTY26JUNFUT" in required
    assert selected_ce in required
    assert selected_pe in required
    assert optional_ce not in required
PY
      - name: Prove both regressions fail before correction
        run: |
          if pytest -q tests/strategies/test_runner_symbol_role_gate.py::test_expiry_entry_policy_blocks_before_live_readiness_and_preparation; then
            echo 'Expected late-expiry-gate regression to fail before correction'
            exit 1
          fi
          if pytest -q tests/data/test_mdm_tick_coalescing.py::test_optional_context_option_is_not_recovery_critical; then
            echo 'Expected optional-context recovery regression to fail before correction'
            exit 1
          fi
      - name: Apply narrow production corrections
        run: |
          python - <<'PY'
          from pathlib import Path

          runner = Path('src/nifty_scalper_bot/strategies/runner.py')
          text = runner.read_text()
          old = '''            if signal and signal.action != "HOLD":
                phase = "phase10_signal_execution"
                self._last_strategy_versions[symbol] = current_version
                signal_phase = self._data_phase.get(symbol)
                live_ready, live_ready_reason, live_ready_details = (
'''
          new = '''            if signal and signal.action != "HOLD":
                phase = "phase10_signal_execution"
                self._last_strategy_versions[symbol] = current_version
                signal_phase = self._data_phase.get(symbol)
                entry_symbol = str(signal.symbol or symbol)
                if signal.action in {"BUY", "SELL"} and is_nifty_option_symbol(
                    entry_symbol
                ):
                    expiry_blocked, expiry_reason = expiry_theta_block()
                    if expiry_blocked:
                        self._emit_runner_eval_decision(
                            symbol=entry_symbol,
                            stage="phase10_entry_policy",
                            reason=expiry_reason,
                            allowed=False,
                            trace_id=trace_id,
                        )
                        self._emit_no_trade_decision(
                            symbol=entry_symbol,
                            trace_id=trace_id,
                            category="entry_session_policy",
                            reason=expiry_reason,
                        )
                        return
                live_ready, live_ready_reason, live_ready_details = (
'''
          if text.count(old) != 1:
              raise SystemExit(f'runner phase10 anchor count={text.count(old)}')
          runner.write_text(text.replace(old, new, 1))

          mdm = Path('src/nifty_scalper_bot/data/market_data_manager.py')
          text = mdm.read_text()
          old = '        add(self._basket_value(basket, "option_symbols", None))\n'
          new = '        # Nearby option strikes are context-only; the selected pair owns recovery.\n'
          if text.count(old) != 1:
              raise SystemExit(f'mdm option requirement anchor count={text.count(old)}')
          mdm.write_text(text.replace(old, new, 1))
          PY
      - name: Validate focused regressions
        run: |
          python -m compileall -q src/nifty_scalper_bot/strategies/runner.py src/nifty_scalper_bot/data/market_data_manager.py
          pytest -q tests/strategies/test_runner_symbol_role_gate.py
          pytest -q tests/data/test_mdm_tick_coalescing.py
          pytest -q tests/risk/test_expiry_gate.py tests/core/test_polling_failover.py
      - name: Validate full repository
        run: pytest -q
      - name: Commit clean targeted correction
        run: |
          git checkout origin/main -- .github/workflows/validate-market-aware-optimisations.yml
          git checkout origin/main -- .github/workflows/strategy-stall-fix-once.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add src/nifty_scalper_bot/strategies/runner.py \
                  src/nifty_scalper_bot/data/market_data_manager.py \
                  tests/strategies/test_runner_symbol_role_gate.py \
                  tests/data/test_mdm_tick_coalescing.py \
                  .github/workflows/validate-market-aware-optimisations.yml \
                  .github/workflows/strategy-stall-fix-once.yml
          git commit -m 'fix(runtime): pre-gate expiry entries and narrow feed recovery'
          git push origin HEAD:fix/entry-policy-critical-feed-20260804
