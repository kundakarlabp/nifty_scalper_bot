name: Diagnose PR889 Python 3.12 full suite

on:
  pull_request:
    branches: [main]
    types: [synchronize]

permissions:
  contents: read

jobs:
  diagnose:
    runs-on: ubuntu-latest
    env:
      BRACKET_AUTO_RESTORE: 'false'
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 50
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - name: Install exact official dependencies
        run: |
          python -m pip install --upgrade pip -q
          python -m pip install -e '.[dev]' -q
          python -m pip install -r dashboard/requirements.txt -q
      - name: Run exact official suite and capture output
        shell: bash
        run: |
          set +e
          python -m pytest -q --tb=long > /tmp/python312-full-suite.txt 2>&1
          echo $? > /tmp/python312-full-suite.status
          exit 0
      - uses: actions/upload-artifact@v4
        with:
          name: pr889-python312-full-suite
          path: |
            /tmp/python312-full-suite.txt
            /tmp/python312-full-suite.status
      - name: Preserve result
        run: exit "$(cat /tmp/python312-full-suite.status)"
