name: One-shot runtime readiness E2E order

on:
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  apply-e2e-order-fix:
    if: github.head_ref == 'fix/runtime-readiness-history-integrity-20260805'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - name: Install project
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e '.[dev]'
      - name: Correct explicit runtime ownership ordering
        run: python .github/scripts/apply_runtime_readiness_e2e_order_fix.py
