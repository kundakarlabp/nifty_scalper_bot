name: One-shot ATM basket hysteresis TDD

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: write

jobs:
  apply-targeted-correction:
    if: github.head_ref == 'fix/atm-basket-hysteresis-20260804'
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/atm-basket-hysteresis-20260804
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip

      - name: Install project
        run: |
          python -m pip install --upgrade pip -q
          python -m pip install -e '.[dev]' -q

      - name: Prove regression, apply exact patch, validate and commit
        run: python tools/tmp_apply_atm_hysteresis_pr.py
