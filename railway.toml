[build]
builder = "NIXPACKS"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
# CRITICAL: Use bash -c wrapper for shell commands
startCommand = "/bin/bash /app/entrypoint.sh"

[env]
source = ".env"
