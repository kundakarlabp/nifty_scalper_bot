[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python -m uvicorn nifty_scalper_bot.core.app:app --host 0.0.0.0 --port ${PORT:-8080}"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Auto-load environment variables from .env file
[env]
source = ".env"
