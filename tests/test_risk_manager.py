"""Legacy risk manager tests kept for reference but skipped by default."""

import pytest

pytest.skip(
    "Legacy risk manager covered by new risk manager suite; skip outdated tests.",
    allow_module_level=True,
)
